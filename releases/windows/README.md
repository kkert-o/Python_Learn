# Windows 便携版

`PythonLearner-1.1.0-portable.zip` 是已经打包好的 Windows 程序。

## 使用

1. 解压整个 ZIP。
2. 双击 `PythonLearner.exe`。
3. 保留 `_internal` 目录及其他文件，不要只复制 EXE。

## 说明

- 此目录只放用于分发的程序包。
- 源码位于仓库根目录的 `windows-exe/`。
- Android App 源码位于仓库根目录的 `android-app/`。
